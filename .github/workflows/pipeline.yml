name: Main
on: push

jobs:
  test_job:
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: echo Hello - this is first step
      - name: Second step
        run: echo Second step
